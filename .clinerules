# =====================================================================
# Academic-GraphRAG · L1 宪法 (Constitution Layer)
# =====================================================================
# 本文件定义了项目最高级别的编码、架构与协作约束。
# 任何对项目代码的修改、重构或扩展都必须遵循以下规则。
# =====================================================================

## 1. 核心技术栈约束 (Tech Stack Constraints)

### 1.1 语言与运行时
- **Python >= 3.11** 是唯一允许的编程语言。
- 不允许引入 Java、Node.js、Go 等非 Python 依赖。
- 所有 Python 代码必须兼容 CPython 解释器。

### 1.2 框架生态
- **LlamaIndex** 为唯一的 RAG 框架选型：
  - `PropertyGraphIndex` 用于知识图谱构建
  - `ReActAgent` 用于智能体编排
  - `FunctionTool` 用于工具函数封装
  - `OpenAILike` 用于兼容非 OpenAI 大模型接入
- 不允许同时混用 LangChain 或 Haystack 等竞品框架。

### 1.3 大模型 (LLM)
- **DeepSeek-v4-pro** 为主推理模型，通过 `OpenAILike` 接口接入。
- API Base: `https://api.deepseek.com/v1`
- Embedding 模型固定为 **BAAI/bge-small-zh-v1.5**，通过 HuggingFace 加载。
- 国内镜像源：`https://hf-mirror.com`，环境变量 `HF_ENDPOINT` 必须在导入 `transformers` / `huggingface_hub` 之前设置。

### 1.4 图数据库
- **Neo4j** 为唯一图数据库选型。
- 连接参数：`bolt://127.0.0.1:7687`，凭证从 `.env` 加载。
- 必须安装 Neo4j Desktop 并启用 APOC 插件。
- 图存储接口：`Neo4jPropertyGraphStore` from `llama_index.graph_stores.neo4j`。

### 1.5 外部工具
- **ArXiv API**：通过 `arxiv` Python 包接入，封装为 `FunctionTool`。
- 所有外部 API 调用必须经过 LlamaIndex 的 FunctionTool 层，不允许直接裸调用。

### 1.6 编码规范
- 所有 `.py` 文件使用 **UTF-8** 编码，文件头不允许省略 `# -*- coding: utf-8 -*-`。
- Windows 环境需显式设置 `sys.stdout` 编码，见 `test_agent.py` 示例。
- 敏感信息（API Key、密码）必须通过 `.env` 加载，禁止硬编码。
- 所有中文输出需确保编码兼容，建议使用 `io.TextIOWrapper` 包裹。

---

## 2. 图谱演化与真值维护协议 (TKG Protocol)

**CRITICAL**: 严禁覆盖或删除旧数据，所有 L2 读写必须严格遵循 Append-Only 机制：

- **[Write] 强制时间戳**：写入新状态必须以 `[YYYY-MM-DD]` 开头。
- **[Invalidate] 状态失效**：发现旧结论错误或过时，向该实体追加带 `[REVERTED]` 或 `[DEPRECATED]` 标签的新 observation。
- **[Evolve] 实体继承**：因重构导致节点失效，使用 `SUPERSEDED_BY` 建立关系链。
- **[Read] 防诈尸读取**：从新到旧溯源，遇到 `[DEPRECATED]/[REVERTED]` 立即阻断，严禁继续采用旧记录。
- **[HumanReadable] 数据可读性**：所有 JSON 数据文件（如 academic-memory.json）必须采用多行缩进格式存储，使每个实体/关系的结构一目了然。禁止单行压缩 JSON。Relation 对象的字段顺序必须为 `type` → `from` → `to` → `relationType`，Entity 对象的字段顺序必须为 `type` → `name` → `entityType` → `observations`，以增强人工可读性。

---

## 3. 目录结构约束 (Directory Layout)

```
GraphRAG/
├── tools/        # FunctionTool 封装：外部工具函数
├── utils/        # 工具函数、实体消歧、内存安全处理
├── scripts/      # 运维脚本：数据库清理、数据迁移
├── data/         # 数据目录：PDF 文献、评估数据集
├── tests/        # 单元测试（待创建）
└── *.py          # 流水线入口文件，禁止放在子目录中
```

## 4. 版本控制规范 (Git)

- 禁止将 `.env`, `.venv/`, `__pycache__/`, `.DS_Store`, 以及大型二进制文件（>100MB）提交到 Git。
- Commit message 格式：`[<模块>] <中文描述>`，例如 `[build_graph] 修复 Neo4j 连接超时问题`。
- 主分支保护：禁止直接向 `main` 分支推送，必须通过 PR/MR 合并。

## 5. 安全与合规

- API Key 泄露后立即撤销并轮换。
- Neo4j 数据库不暴露到公网，仅本地 127.0.0.1 访问。
- 所有用户输入必须经过字符串 sanitize，防止 Cypher 注入。
